{
  "name": "task-management-mean",
  "version": "1.0.0",
  "private": true,
  "description": "MEAN stack task management application with JWT auth, role-based authorization, task CRUD, and real-time updates.",
  "scripts": {
    "install:all": "npm --prefix backend install --include=dev && npm --prefix frontend install --include=dev",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend start",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "start": "npm --prefix backend start",
    "test:backend": "npm --prefix backend test",
    "seed": "npm --prefix backend run seed"
  },
  "engines": {
    "node": "22.x"
  }
}
